import { Routes } from '@angular/router';
import { HomePageTableComponent } from './home-page-table/home-page-table.component';
export const routes: Routes = [
    {path: '', component: HomePageTableComponent},
];
